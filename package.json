{
  "name": "@indoor-analytics/random-lines-clustering",
  "version": "0.0.1",
  "description": "Clusters a bunch of paths using random-picked lines.",
  "main": "main.js",
  "types": "main.d.ts",
  "scripts": {
    "build": "npx tsc",
    "prepare": "npm run build",
    "test": "npm run build && mocha -r ts-node/register test/tests/*.test.ts"
  },
  "author": "Rémy Raes",
  "license": "ISC",
  "devDependencies": {
    "@types/chai": "^4.2.11",
    "@types/mocha": "^7.0.2",
    "@types/node": "^14.0.1",
    "chai": "^4.2.0",
    "mocha": "^7.1.2",
    "ts-node": "^8.10.1",
    "tslint": "^6.1.3",
    "typescript": "^3.8.3"
  },
  "dependencies": {
    "@turf/helpers": "^6.3.0",
    "geojson": "^0.5.0"
  }
}
